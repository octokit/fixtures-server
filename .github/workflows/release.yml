name: Release
"on":
  push:
    branches:
      - master
      - next
      - beta
      - v*.x
jobs:
  build_matrix:
    strategy:
      matrix:
        os: [windows, macos, ubuntu]
        include:
          - os: windows
            build: npx caxa --input . --output dist/fixtures-server-win.exe -- "{{caxa}}/node_modules/.bin/node" "{{caxa}}/bin/server.js"
          - os: macos
            build: npx caxa --input . --output dist/fixtures-server-macos -- "{{caxa}}/node_modules/.bin/node" "{{caxa}}/bin/server.js"
          - os: ubuntu
            build: npx caxa --input . --output dist/fixtures-server-linux -- "{{caxa}}/node_modules/.bin/node" "{{caxa}}/bin/server.js"
    runs-on: ${{ matrix.os }}-latest
    steps:
      - uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b # tag=v3
      - uses: actions/setup-node@5b949b50c3461bbcd5a540b150c368278160234a # tag=v3
        with:
          node-version: 16
      - run: npm ci
      - run: ${{ matrix.build }}
      - uses: actions/upload-artifact@3cea5372237819ed00197afe530f5a7ea3e805c8 # tag=v3
        with:
          name: build-artifacts
          path: dist

  release:
    name: release
    needs: [build_matrix]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@2541b1294d2704b0964813337f33b291d3f8596b # tag=v3
      - uses: actions/setup-node@5b949b50c3461bbcd5a540b150c368278160234a # tag=v3
        with:
          node-version: 16
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@fb598a63ae348fa914e94cd0ff38f362e927b741 # tag=v3
        id: download
        with:
          name: build-artifacts
          path: dist
      - run: ls dist
      - run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
